import type { NextPage } from 'next';
import React from 'react';

const emailVerification: NextPage = () => {
	return (
		<>
			<h1>Check your email for a sign in link</h1>
			<p>redirect to home page</p>
		</>
	);
};

export default emailVerification;
